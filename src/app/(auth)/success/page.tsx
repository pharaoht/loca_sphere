'use client'

import useParams from "@/hooks/useParams"

const Page = () => {
    //a way to access params
    const { getParam } = useParams();
    //state

    //get token from param

    //save in local storage

    //fetch user data with token
    return <div>
        hi
    </div>
}

export default Page